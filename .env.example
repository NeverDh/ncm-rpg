# PostgreSQL (docker-compose padrão deste repo)
DATABASE_URL="postgresql://rpg:rpg@localhost:5432/rpg?schema=public"

# BotFather — obrigatório para polling do Telegraf
BOT_TOKEN=

# HTTP (Swagger + health)
PORT=3000
